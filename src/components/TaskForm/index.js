import React from 'react';
import Button from '../Buttons';
import Input from '../Input';

const TaskForm = () => {
    return (
        <form>
            <Input />
            <Button>ADD</Button>
        </form>
    );
};

export default TaskForm;
